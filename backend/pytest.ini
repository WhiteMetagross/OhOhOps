[pytest]
# The offline, deterministic suite lives in tests/. The live smoke scripts in
# scripts/ are named test_*.py but require real Gemini/Pinecone/Docker, so they
# are intentionally excluded from automated collection.
testpaths = tests
